<h1>Editing Task <%= @task.name %></h1>
<%= link_to sanitize('Back to ' + Project.find_by_id(@task.projectid).name), project_path(@task.projectid) %>
<hr>
<%= button_to 'Remove Task', @task, method: :delete, data: { confirm: 'Are you sure you want to remove this from your tasks?' }, :class => "mobilelarge" %>
<%= render 'form' %>